# build-own-angular-library
#Steps
- Create Angular workspace using
  * ng new workspace-name
- After creating workspace then create library workspace in project folder
  * ng generate library library-name
